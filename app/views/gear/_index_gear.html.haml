.unit
  .col.thumb.first
    = gear_link(index_gear, image_tag(index_gear.image.url(:thumb)))
  .col.desc
    %h4
      = gear_link(index_gear)
    %small= render(:partial => index_gear.class.index_partial, :object => index_gear)
    - if @chain
      = add_to_chain_button(index_gear)
  %br.clear
