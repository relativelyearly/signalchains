.comment.first
  .first.col.comment-text.w45
    =textilize(comment.body)
  .w20.col
    .avatar.col.snug
      = image_tag comment.user.avatar.url(:thumb)
  
    - if comment.user
      .col.w10
        = comment.user.login
  .clear      
  