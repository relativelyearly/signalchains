.comment.first
  .first.col.comment-text.w45
    =textilize(comment.body)
  .w20.col
    .avatar.col.snug
      = link_to image_tag(comment.user.avatar.url(:thumb)), comment.user
  
    - if comment.user
      .col.w10
        = link_to comment.user.login, comment.user
  .clear      
  