.event.new_chain
  %p
    = link_to new_chain.actor.display_name, user_by_login_path(new_chain.actor.login)
    created a chain called
    %strong= link_to new_chain.subject.name, new_chain.subject
  %small.timestamp= new_chain.created_at.to_s :us_with_time