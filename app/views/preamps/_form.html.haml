- semantic_form_for @preamp, :html => {:multipart => true} do |f|
  = render(:partial => 'gear/form', :locals => {:f => f})

  = f.input :circuitry, :as => :select, :collection => Preamp.circuitries
  - if @chain
    = hidden_field_tag :chain_id, @chain.id
  = f.buttons