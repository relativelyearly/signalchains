== Logged in as #{@user.email}
%br
= link_to 'Edit', edit_user_url(@user), :class => 'button'
= follow_button(@user)

%h2 Events
- for event in @events
  = render(:partial => "events/#{event.event_type}", :object => event)