%h3 Create a New Equalizer
%p.borderB
  Users will be able to use this piece of gear after you create it. 
  %br
  All fields are not required, but you won't be able to edit them later. 

- semantic_form_for @equalizer, :html => {:multipart => true} do |f|
  .col.first.w45= render(:partial => 'gear/form', :locals => {:f => f})
  .col.w15
    = f.input :bands, :input_html => { :size => 10 }  
    = f.input :equalizer_type, :as => :select, :collection => Equalizer.equalizer_types, :label => 'Type'
    = f.input :interface, :as => :select, :collection => Equalizer.interfaces
    - if @chain
      = hidden_field_tag :chain_id, @chain.id
  .clear
  = f.buttons